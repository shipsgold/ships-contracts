{
  "openrpc":"1.2.4",
  "info": {
    "version": "0.0.1",
    "description": "Ships Contract",
    "title": "ShipsContract"
  },
  "methods": [
    {
      "name": "increment",
      "params": [],
      "tags": [{
        "$ref": "#/components/tags/change"
      }],
      "result":{
        "name": "count",
        "schema": {
          "type":"number"
        } 
      }
    },
    {
      "name": "add_guest",
      "params": [{
        "$ref": "#/components/contentDescriptors/pubKey"
      }],
      "tags": [{
        "$ref": "#/components/tags/change"
      }],
      "result":{
        "name":"GuestResult",
        "schema": {
          "type":"null"
        }
      }
    },
    {
      "name": "get_count",
      "params": [],
      "tags": [ {
          "$ref": "#/components/tags/view"
        }],
        "result": {
          "name": "countResult",
          "schema": {
            "type": "number"
          }
        }
    },
    {
      "result": {
        "name": "InitResult",
        "required": false,
        "schema": {
          "type": "null"
        }
      },
      "name": "new",
      "description": "initialization method",
      "tags": [{
        "$ref": "#/components/tags/change"
      }],
      "params": [
        {
          "name": "owner_id",
          "required": true,
          "schema": {
            "title": "owner_id",
            "type": "string"
          }
        },
        {
          "name": "count",
          "required": true,
          "schema": {
            "title": "count",
            "type":"number"
          }
        }
      ]
    }


  ],
  "components": {
    "tags": {
      "change" : {
        "name": "change",
        "description": "change method"
      },
      "view" : {
        "name": "view",
        "description": "view method"
      }
    },
    "contentDescriptors": {
     "pubKey": {
       "name": "access_key",
       "schema": {
         "title": "PubKey",
         "type": "string"
       },
       "required": true
     } 
    },
    "schemas": {
      "PubKey": {
        "title": "pub_key",
        "schema": {
          "type": "string"
        }
      }
    }
  }
}